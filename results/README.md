# Results of analyses

Should be small files (generally summary outputs etc) so they can be kept on GitHub.

Should contain a set of folders that correspond to the scripts in the `code` folder than generated them (ideally the same name as the script, e.g. `results/s01_process_data`). 
